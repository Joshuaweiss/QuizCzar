<canvas class="Chart" width="590" height="200"></canvas>

<%

  setTimeout(function () {
    $(".Chart").each(function(){
      ctx = this.getContext("2d");
      ctx.width = 590;
      ctx.height = 200;
      var lineChart = new Chart(ctx).Line(data, {
        scaleOverride: true,
        scaleStartValue: 0,
        scaleSteps: 5,
        scaleStepWidth: 20
      });
    });
  }, 0);


%>
